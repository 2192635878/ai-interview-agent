# AI 模拟面试官对话记录

## 面试官
好的，欢迎！我们这次面试的目标是帮助你把 Golang 的一些核心基础打扎实，所以我会先给你讲一个小知识点，然后再结合它出一道练习题。你回答完之后我会给你点评、打分，并继续追问，帮你一步步深入。

---

### 知识点讲解：Goroutine 与并发入门

在 Go 里，**goroutine** 是并发执行的轻量级线程。它由 Go 运行时管理，比操作系统线程更轻量（栈初始只有几 KB，且可以动态伸缩）。启动一个 goroutine 非常简单，只需要在函数调用前加上 `go` 关键字。

例如：
```go
go doSomething()
```

这样就可以让 `doSomething` 和调用它的代码**同时**运行（并发），不阻塞后续逻辑。

但是，goroutine 本身不直接返回结果，多个 goroutine 之间通常需要用 **channel（通道）** 来通信和同步。这就是 Go 著名的理念：

> “不要通过共享内存来通信，而应通过通信来共享内存。”

---

### 第一道简单练习题

请补全下面的代码，让两个 goroutine 并发地向同一个 channel 发送字符串 “hello” 和 “world”，主 goroutine 从 channel 接收并打印这两条消息，最终程序正常结束。

要求：
1. 使用 **无缓冲 channel**。
2. 两个 goroutine 分别发送 “hello” 和 “world”。
3. 主 goroutine 接收两次并打印（顺序可以不同，只要能都打印出来）。
4. 不要使用 `sync.WaitGroup` 等额外同步手段（只用 channel 的特性来等待）。

```go
package main

import "fmt"

func main() {
    ch := make(chan string) // 无缓冲 channel

    // 你的代码：启动两个 goroutine，分别向 ch 发送 "hello" 和 "world"
    // ...

    // 你的代码：从 ch 接收两次并打印
    // ...
}
```

请你尝试写出完整的 `main` 函数。直接给出代码即可，同时简单说明一下为什么这样写能保证程序正常退出。

## 我
我好像完全不懂，能教教我吗

## 面试官
没问题，完全不懂是很正常的，我们从最基础的地方开始，一步一步来。

---

### 通俗讲解：什么是 Goroutine 和 Channel

想象你在厨房里，你需要烧水和切菜。如果只有你一个人（相当于一个“线程”），你必须先烧水，等水开了再切菜，这就是**串行**。

但如果你可以同时做这两件事（一边烧水，一边切菜），这就是**并发**。在 Go 里，你可以给每件事安排一个 **goroutine**，它就像一个很小的“小助手”，可以独立去干活。

启动一个 goroutine 非常简单，就是在一个函数调用前面加上 `go`：
```go
go 烧水()
go 切菜()
```
这样，`烧水`和`切菜`就会同时进行，不会互相阻塞。

但是，如果两个助手干完活，要把结果告诉“你”（主 goroutine），你怎么拿到结果呢？这时候就需要 **channel**（通道），你可以把它想象成一根水管，一个助手把结果从这头塞进去，你从那头接收。

- **无缓冲 channel**（`make(chan 类型)`）：水管中没有存水的地方，必须有人同时准备接，才能把水传过去。也就是说，**发送方必须等到有接收方准备好**，反之亦然。这可以自然地实现同步。

在我们的练习里，两个 goroutine 是发送方，主 goroutine 是接收方。因为无缓冲 channel 要求发送和接收必须同时发生，所以我们可以利用这个特性来保证所有 goroutine 都正常工作，并且程序最后能正常退出。

---

### 更简单的小练习

先不要求写代码，我们来理解一个更基础的情况：**单个 goroutine 通过 channel 发送一个值，主 goroutine 接收并打印**。

请你看下面的代码，并思考：

```go
package main

import "fmt"

func main() {
    ch := make(chan string)

    go func() {
        ch <- "hello" // 发送
    }()

    msg := <-ch // 接收
    fmt.Println(msg)
}
```

**问题**：为什么程序能正常打印出 “hello” 并且退出，不会卡住？  
（提示：想想“无缓冲 channel 的发送和接收必须在同一时间准备好”）

你可以用自己的话简单解释一下。不用紧张，用生活化的比喻也可以。
