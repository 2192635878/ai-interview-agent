# AI 模拟面试官对话记录

## 我
请开始面试，先根据岗位提出第一道技术问题。

## 面试官
好的，我们开始面试。

第一个问题：请解释一下 Java 中 HashMap 的底层数据结构，以及它是如何解决哈希冲突的？

## 我
hashmap的底层结构是哈希表，它是一种根据特征自动转换为地址的特殊表结构，当它把特征变为数据，再对其进行模运算，可以通过其位置得到其信息，且已知其信息，也可以快速得到所在位置，是一种非常好用的O（1）查询结构，当发生哈希冲突时，会使用数组将其存储在一起，当查询到冲突时，会在该位置逐个查询。所以当哈希表完全冲突时，就从哈希表退化成链表。

## 面试官
回答评价：你对 HashMap 的基本概念有一定理解，提到了哈希表、O(1) 查询、冲突时用数组存储等关键点，但表述不够准确，存在概念混淆。

存在问题：
1. “根据特征自动转换为地址” 这种描述不够专业，应该明确是“通过 hashCode() 方法计算哈希值，再通过哈希函数映射到数组下标”。
2. “使用数组将其存储在一起” 是错误的，HashMap 解决哈希冲突的方式是“链表 + 红黑树”，而不是数组。JDK 1.8 之后，当链表长度超过阈值（默认 8）时，会转换为红黑树。
3. 你没有提到 HashMap 的初始容量、负载因子、扩容机制等关键细节。

改进建议：建议你重新梳理 HashMap 的核心结构，重点掌握：数组 + 链表/红黑树的组合结构，以及 put 和 get 的完整流程。

参考思路：
- HashMap 底层是一个 Node<K,V>[] 数组，每个数组元素是一个链表或红黑树的头节点。
- 当插入键值对时，先计算 key 的 hashCode，再通过 (n - 1) & hash 计算数组下标。
- 如果该位置为空，直接放入；如果不为空，则遍历链表或红黑树，如果找到相同 key 则覆盖，否则在尾部插入（JDK 1.8 尾插法）。
- 当链表长度 >= 8 且数组长度 >= 64 时，链表转为红黑树，提高查询效率。

下一题：基于你刚才的理解，请问 HashMap 在什么情况下会触发扩容？扩容时具体做了哪些操作？
